# Keep normal recovery bootable after mkfs.btrfs changes the filesystem UUID.
# The recovery contract verifies that generated grub.cfg uses PARTUUID or LABEL.
GRUB_DISABLE_LINUX_UUID=true
GRUB_DISABLE_LINUX_PARTUUID=false
